<!DOCTYPE html>
<html>
  <head>
    <title>TravelJournal</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <!-- Make sure your custom.css is included. If it's in app/assets/stylesheets, this line handles it. -->
  </head>

  <body>
    <div class="container mt-3">
      <nav class="navbar navbar-expand-lg navbar-light">
       <div class="container-fluid">
        <%= link_to "Home", root_path, class: "navbar-brand" %>
         <div class="ms-auto">
          <ul class="navbar-nav">
           <% if @current_user %>
            <li class="nav-item me-3">
             <span class="navbar-text">Logged in as: <%= @current_user.email %></span>
            </li>
            <li class="nav-item">
            <%# This is the corrected logout link for Rails 7+ %>
            <%= button_to "Logout", "/logout", method: :delete, class: "nav-link" %>
            </li>
           <% else %>
            <li class="nav-item me-3">
             <%= link_to "Login", "/login", class: "nav-link" %>
            </li>
            <li class="nav-item">
             <%= link_to "Signup", "/signup", class: "nav-link" %>
            </li>
           <% end %>
           </ul>
         </div>
        </div>
       </nav>

       <hr/>

    <div class="container">
     <%# The rest of your page content will be rendered here %>
     <%= yield %>
    </div>

    <!-- Bootstrap JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>

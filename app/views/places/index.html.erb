<h1 class="display-4">Places</h1>

<div class="my-4">
  <%= link_to "New Place", new_place_path, class: "btn-custom" %>
</div>

<ul class="places-list">
  <% @places.each do |place| %>
    <li>
      <%= link_to place.name, place_path(place) %>
    </li>
  <% end %>
</ul>